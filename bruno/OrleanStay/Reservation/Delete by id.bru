meta {
  name: Delete by id
  type: http
  seq: 4
}

delete {
  url: http://localhost:8080/reservation/9
  body: none
  auth: none
}

script:pre-request {
  const cookie = bru.getVar("cookie");
  
  if(cookie) {
    req.setHeader("Cookie", cookie)
  }
  
  const token = bru.getVar("authorization");
  
  if(token) {
    req.setHeader("authorization", token);
  }
}
